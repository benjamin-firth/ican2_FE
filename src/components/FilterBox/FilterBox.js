import React from 'react';
import './FilterBox.scss';

const FilterBox = () => {
  window.scrollTo(0, 0);

  return (
    <section className='filter-container'>
      <h5>this is the filter box</h5>
    </section>
  );
}

export default FilterBox;
